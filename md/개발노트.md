### 260414 화
* .env -> gitignore
* psql 연결 완료 및 notes 테이블 작성 완료
* sudo docker compose exec postgres psql -U omelet1 -d dxnote
  * psql 접속 명령어

### 260410 금
* save 임시 버튼 구현 완료 (로직은 미완성)

### 260409 목
* 인라인 css -> main.css로 분리
* App() -> Note() 컴포넌트 분리
* 테스트용 Button() 컴포넌트 만드는 중 (~ing)
### 260408 수
* 데비안에서 띄울시 한글 입력 안되는 문제가 발생  
  * 1. GTK_IM_MODULE=ibus dx serve
    * 해결되었으나 이중 입력창(Floating Preedit) 문제 발생
  * 2. 비제어 컴포넌트 방식
    * value: "{text_value}" -> 주석 처리를 통해 리눅스 입력기와 충돌 방지
* MacOS에 맞게 with_always_on_top(false) 적용
* docker-compose.yml 작성완료 및 실행완료
### 260402 목
* 도커 설치 완료
* PostgreSQL 도커 이미지 설치 완료
### 260331 화
* 로직 짜기
  * 빈 메모장 화면 만들기
  * PostreSql 설치
  